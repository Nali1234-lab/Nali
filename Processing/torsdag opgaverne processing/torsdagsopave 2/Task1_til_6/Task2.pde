/*boolean happy = true; 

void setup() {
  
  if (iAmHappy()) {
    println("I clap my hands");
  } else {
    println("I don't clap my hands");
  }

  // 2.e: Kald metoder og gem returværdier i variabler
  int sumResult = addNumbers(5, 10);
  String upperCaseText = toUpperCase("hello");
  boolean isUpperCase = startsWithUpperCase("Hello");

  // 2.e: Print resultater
  println("Sum: " + sumResult);
  println("Uppercase: " + upperCaseText);
  println("Starts with uppercase: " + isUpperCase);
}

// 2.a: missing word "happy".
boolean iAmHappy() {
  return happy;
}

// 2.b: Metode der returnerer summen af to tal
int addNumbers(int a, int b) {
  return a + b;
}

// 2.c: Metode der returnerer en String i uppercase
String toUpperCase(String text) {
  return text.toUpperCase();
}

// 2.d: Metode der tjekker om første bogstav i en String er stort
boolean startsWithUpperCase(String text) {
  if (text.length() == 0) {
    return false; // Håndter tom tekst
  }
  char firstChar = text.charAt(0);
  return Character.isUpperCase(firstChar);
}
*/
