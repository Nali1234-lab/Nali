/*class Teacher {
 // Instance variabler
 String name;
 int age;
 boolean isFemale;
 
 // Constructor
 Teacher(String tmpName, int tmpAge, boolean tmpIsFemale) {
 this.name = tmpName;
 this.age = tmpAge;
 this.isFemale = tmpIsFemale;
 }
 
 // 4.a - Metode til at ændre lærerens navn
 void changeName(String newName) {
 this.name = newName;
 }
 }
 
 void setup() {
 // 4.b - Opret en Teacher-instans
 Teacher myTeacher = new Teacher("Tess", 25, false);
 
 // Print lærerens navn før ændring
 println("Teacher (before change): " + myTeacher.name);
 
 // 4.b - Ændr lærerens navn
 myTeacher.changeName("Anna");
 
 // 4.c - Print lærerens navn efter ændring
 println("Teacher (after change): " + myTeacher.name);
 }
 */
