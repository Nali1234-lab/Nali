/*void setup() {

  size(600, 600);  // Sætter vinduets størrelse
  background(200); // Baggrundsfarve (grå)
  int count = 0;  // Tæller antallet af cirkler


  while (count < 100) {
    float x = random(width);   // Tilfældig x-position
    float y = random(height);  // Tilfældig y-position
    float d = random(10, 225); // Tilfældig diameter mellem 10 og 225
    int fillColor = color(random(255), random(255), random(255)); // Tilfældig farve
    int strokeColor = color(random(255), random(255), random(255)); // Tilfældig kantfarve

    fill(fillColor);
    stroke(strokeColor);
    strokeWeight(2);

    ellipse(x, y, d, d); // Tegner cirklen

    count++; // Øger tælleren
  }
}
*/
